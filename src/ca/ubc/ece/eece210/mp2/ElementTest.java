package ca.ubc.ece.eece210.mp2;

import static org.junit.Assert.*;

import org.junit.Test;

import ca.ubc.ece.eece210.mp2.Element.Node;

public class ElementTest {

    @Test
    public void testAddChild() {
        Node root = null;
        Node newChild = null;
        Node focusNode = null;
        
        if (root == null) {
            root = newChild;


    assertEquals(root, focusNode); 
    }
    }
}
